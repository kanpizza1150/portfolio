import styled from 'styled-components'

const Title = styled.div`
  font-weight: bold;
  text-transform: uppercase;
  display: flex;
`
const Logo = () => {
  return <Title>Kanpichcha 🍕</Title>
}

export default Logo
